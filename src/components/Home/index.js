import { Button, Container } from '@mui/material'

const Home = () => {
    return (
        <Container>
            <Button variant={'contained'} > lets go </Button>
        </Container>
    )
}

export default Home
