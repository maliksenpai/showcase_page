import { Button, Container } from '@mui/material'

const Home = () => {
    return (
        <Container>
            <Button variant={'contained'}> Deneme </Button>
        </Container>
    )
}

export default Home
